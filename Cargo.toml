[package]
name = "lark-rust"
version = "0.1.1"
edition = "2021"

[lib]
name = "lark_rust"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.25", features = ["extension-module"] }
fancy-regex = "0.14"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = true
